services:
  - type: web
    name: transport-gadget-classifier-api
    env: python
    buildCommand: pip install -r backend/requirements.txt
    startCommand: gunicorn backend.app:app --bind 0.0.0.0:$PORT --timeout 120 --workers 1
    healthCheckPath: /health
